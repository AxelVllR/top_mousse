{% extends 'base.html.twig' %}

{% block title %}Top Mousse - {{ page.title }}{% endblock %}

{% block body %}
    <div class="mainContent">
        <div class="container">
            <div class="breadcrumb">
                <ul>
                    <li>
                        <a href="{{ path('index') }}">
                            Accueil
                        </a>
                    </li>
                </ul>
            </div>
            <div class="contentPage">
                <h1>{{ page.title }}</h1>
                {{ page.content | raw }}
            </div>
        </div>
    </div>
{% endblock %}